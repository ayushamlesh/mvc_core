@*it will let the view know what is receiverd is the type of IEnmurable.*@
@model IEnumerable<Category>

@{
    ViewData["Title"] = "Index";
}

@*we are capturing the data passed from controller*@
<div class="container p-3"> 
    @*parts are devided into 12 sections*@
    <div class="row pt-4"> 
        @*compining 6 in this and another 6 below*@
        <div class="col-6"> 
            <h2>Category List</h2>
        </div>
        <div class="col-6 text-end" >
            <a asp-controller="Category" asp-action="Create" class="btn btn-primary">
                <i class="bi bi-file-earmark-plus"></i>   Create New Category
            </a>
        </div>
    </div>


<table class="table table-bordered table-striped" style="width:100%">
     <thread>
         <tr>
            <th>BatchId</th>
            <th>Medecine Name</th>
            <th>Quantity</th>
            <th>Price</th>
          
     </thread>
     
     <tbody>
    @foreach(var items in Model)
        {
            <tr>
                <td width="20%">@items.CatId</td>
                <td width="30%">@items.CatName</td>
                <td width="20%">@items.Quantity</td>
                <td width="30%">@items.Price</td>
                <td>
                  @*  adding button for edit and delete*@
                    <div class="m-75 btn-group" role="group">
                        <a asp-controller="Category" asp-action="Edit" asp-route-id="@items.CartId" class="btn btn-primary mx-2">
                            <i class="bi bi-pencil-square"></i>Edit
                        </a>                
                            <a asp-controller="Category" asp-action="Delete"  asp-route-id="@items.CartId" class="btn btn-danger">
                                <i class="bi bi-trash"></i>Delete
                        </a>
                    </div>
                </td>

            </tr>

        }
    </tbody>
</table>

</div>
