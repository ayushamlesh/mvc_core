@model IEnumerable<Paharamacyapi.Models.Drugs>
<form method="post">

    @*using the below because when we are updatig the data it is showing the previous on only.*@
    @*<input asp-for="CartId" hidden/>*@
    <div class="border p-3 mt-4">
        <div class="row pb-2">
            <h2 class="text-primary">Order</h2>
            <hr />
        </div>
    <select>
        @foreach (var items in Model)
        {
            <option name="selectedMed" >
                <td width="30%">@items.DrugName</td>
            </option>
        }
    </select>

            <div class="mb-3">
                <lable>Quantity</lable>
                <input name="Quantity" class="form-control" />
             </div>
                    @*  adding button for to proced with order*@
                    <div class="m-75 btn-group" role="group">
            <a asp-controller="Category" asp-action="Order" asp-route-id=DrugName asp-route-data="Quantity" class="btn btn-primary mx-2">
                            <i class="bi bi-pencil-square"></i>Order
                        </a>

                        <a asp-controller="Category" asp-action="Index" class="btn btn-secondary">
                            Back To List
                        </a>    
                    </div>
       

   
</div>
</form>


@*            adding for clientside validation*@

@section Scripts{
    @{
        <Partial name="_ValidationScriptsPartial" />
    }
                }


@*creating serverside validation*@
<div asp-validation-summary="All"></div>

@*    <div class="mb-3">
            <label>Medicine Name</label>
            <select class="form-control" name="CatId" id="catId">
                @foreach (var medicine in Model)
                {
                    <option value="@medicine.CatName">@medicine.CatName</option>
                }
            </select>
        </div>
    *@